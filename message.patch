diff --git a/PythonPackage.cmake b/PythonPackage.cmake
index 0f18a3b..57771f6 100644
--- a/PythonPackage.cmake
+++ b/PythonPackage.cmake
@@ -624,6 +624,7 @@ function(add_setup_py target template)
 
         set(_lnk "")
         foreach (item ${lnk})
+            message(STATUS "LNK ITEM: ${item}")
             get_filename_component(_libdir ${item} DIRECTORY)
 
             if (_libdir)
@@ -632,6 +633,13 @@ function(add_setup_py target template)
                 string(REGEX REPLACE "lib" "" item ${item})
             endif ()
 
+            message(STATUS "Item is :: ${item}")
+
+            if( "${item}" MATCHES "framework")
+                message(STATUS "::::::::::: FOUND FRAMEWORK ${item}")
+                set(item "-framework ${item}")
+            endif()
+
             list(APPEND _lnk "'${item}'")
         endforeach ()
 
